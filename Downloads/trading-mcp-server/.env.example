# Server Configuration
PORT=3001
NODE_ENV=development

# API Keys (Backend security)
API_KEY=Qlp7lgahCNE7IzAOX17qcBcDlV6IX7a4

# Google Gemini
GOOGLE_GEMINI_API_KEY=AIzaSyD78IcOSbP1JAr9N3S1drcAJ8fZfjsDbJQ
GEMINI_MODEL=gemini-2.5-flash
GEMINI_FALLBACK_MODEL=gemini-1.5-flash
GEMINI_TIMEOUT_MS=45000

# Проксируем только Gemini (HTTP/HTTPS прокси вашего VPN)
GEMINI_PROXY_URL=socks5h://127.0.0.1:1080

# Polygon — работаем напрямую (без прокси)
POLYGON_API_KEY=xCOEkBcNd7GfzgK7PVLIp2xMIPzSOWvJ
# Рекомендуется для «узкой» сети увеличить таймауты:
POLYGON_TIMEOUT_MS=12000
POLYGON_MAX_RETRIES=1
POLYGON_BACKOFF_BASE_MS=300

# Принудительный обход прокси для Polygon (на случай системных прокси от VPN)
NO_PROXY=api.polygon.io

# CORS / Frontend
FRONTEND_URL=http://localhost:3000

# Logging
LOG_LEVEL=info

# DB Prisma (SQLite)
DATABASE_URL=file:./prisma/dev.db

# NewsAPI
NEWSAPI_KEY=c1a41da5b3a64784bc7c604798c0e83a
# Выберите язык источников: en или ru
NEWSAPI_ENABLED=true
NEWS_PROXY_URL=socks5h://127.0.0.1:1080
NEWSAPI_LANGUAGE=en
NEWSAPI_MAX_PER_SYMBOL=2
NEWSAPI_TIMEOUT_MS=8000

# Polygon cache TTL overrides (optional)
POLYGON_TTL_MINUTE_MS=3600000
POLYGON_TTL_HOUR_MS=21600000
POLYGON_TTL_DAY_MS=86400000

# FRED (US macro data)
FRED_API_KEY=your_fred_api_key
FRED_TIMEOUT_MS=20000
FRED_MAX_RETRIES=3
# Рекомендуется обходить прокси для FRED:
NO_PROXY=api.polygon.io,api.stlouisfed.org,stlouisfed.org

# Macro prompt controls
# Для minute по умолчанию промпт получает только macroSummary (ряды не передаются)
MACRO_MINUTE_USE_SUMMARY_ONLY=true
# Количество последних точек рядов в промпте/ответе (для hour/day). По умолчанию 3
MACRO_PROMPT_POINTS=3